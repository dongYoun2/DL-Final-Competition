# @package _global_
data:
  dataset_name: cub200
  cache_dir: /gpfs/scratch/dl5635/.cache
  data_dir: /gpfs/data/fieremanslab/dayne/projects/DL-Final-Competition/data/CUB-200

  # Image preprocessing
  image_size: 96  # Match the model's input size
  image_key: img  # Not used for custom dataset

  # Data splits
  train_split: train
  val_split: val
  test_split: test

  # No augmentation (only for evaluation)
  augmentation: null



